CREATE ALGORITHM=UNDEFINED DEFINER=`root`@`localhost` SQL SECURITY DEFINER VIEW `best_low_goal_bot` AS select `match_results`.`team_number` AS `team_number`,avg(`match_results`.`auto_low`) OVER (PARTITION BY `match_results`.`team_number` )  AS `avg_low_auto`,avg(`match_results`.`tele_op_low`) OVER (PARTITION BY `match_results`.`team_number` )  AS `avg_low_tele` from `match_results`